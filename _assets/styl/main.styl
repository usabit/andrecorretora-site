flex-version = flex
support-for-ie = true
vendor-prefixes = official

@import 'nib'
@import 'rupture'
@import 'jeet'

rupture.scale = 0     600px    800px     1200px
rupture.scale-names = 'mobile' 'tablet' 'desktop'

global-reset()

blue = #173c46
orange = #eeaa49

@import "_definitions/*";
@import "_partials/*";
@import "elements/*";
// @import "pages/*";

@-ms-viewport{
  width: device-width;
}

.wrapper {
  width 90%
  max-width 1150px
  margin 0 auto
}